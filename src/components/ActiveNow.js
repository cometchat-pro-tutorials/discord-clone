const ActiveNow = () => {
  return (
    <div className="an">
      <div className="an-title">Active Now</div>
      <div className="an-description">
        <p className="an-description-title">It's quiet for now...</p>
        <p className="an-description-content">When a friend starts an activity - like playing a game or hanging out on voice - we'll show it here!</p>
      </div>
    </div>
  );
};

export default ActiveNow;